@model ApiCursos.models.AlunoModel;
{
    ViewData["Title"] = "Retirar";

<div class="text-center">
        <h3>Deseja retirar o aluno?(@Model.Name)</h3>
</div>

<a class="btn btn-primary btn-block" role="button" asp-controller="Aluno" asp-action="AlunoIndex">cancelar</a>
<a class="btn btn-warning btn-block" role="button" asp-controller="Aluno" asp-action="Apagar" asp-route-itemid="@Model.RA">Confirmar exclusão</a>
}